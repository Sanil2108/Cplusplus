#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    char c;
    FILE *fp=fopen("temp.txt", "w");
    while((c=getchar())!=EOF){
        putc(c, fp);
    }

    fclose(fp);
    fp=fopen("temp.txt", "r");
    while((c=getc(fp))!=EOF){
        printf("%c", c);
    }

    fclose(fp);
    return 0;
}*/
